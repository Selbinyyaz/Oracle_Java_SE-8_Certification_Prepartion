package p1;

public class Acc {
	
	int p; // not accessible outside of the package.
	private int q; //only accessible within the class
	protected int r; // not accessible outside of the package but it only by inheritance
	public int s; // accessible in universe

}
